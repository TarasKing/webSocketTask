{
  "name": "websockettask",
  "version": "1.0.0",
  "description": "home task with webSocket",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "nodemon --watch src --ext ts --exec 'npx tsc && node dist/server.js'",
    "prod": "npm run build && npm start"
  },
  "author": "Taras",
  "license": "ISC",
  "dependencies": {
    "ws": "^8.18.1"
  },
  "devDependencies": {
    "@types/ws": "^8.18.0",
    "nodemon": "^3.1.9",
    "typescript": "^5.8.2"
  }
}
